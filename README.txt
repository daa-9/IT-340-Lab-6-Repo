VM Name: dev-onboarding-daa9
username: dev-ryan
password: tempPass

To run the monitor.sh script, be sure to pull up the terminal (ctrl+alt+t) and access the directory (cd) the script that is stored in:

/home/developers/Lab_6_workspace/scripts/

and run the script with "./monitor.sh" (no quotation marks)

When your shift is over for the day, be sure to run the timesheet.sh script also stored in:

/home/developers/Lab_6_workspace/scripts/

and run the script with "./timesheet.sh" (no quotation marks)

You will be prompted to enter your name, last name, hours worked, and a description of the work you have done that day.
Press enter after inputting the required details, and you're done for the day! Hope your work goes smoothly!
